<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">CRM App</a>

    <div class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Accounts", accounts_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Contacts", contacts_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Models", models_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Opportunities", opportunities_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Reports", reports_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to "Logout", destroy_user_session_path, method: :delete, data: { turbo: false } %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
